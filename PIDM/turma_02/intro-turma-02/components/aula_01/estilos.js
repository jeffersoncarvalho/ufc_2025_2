import {StyleSheet} from "react-native"

const estilos = StyleSheet.create({
    container: {
        backgroundColor: "red"
    },
    texto: {
        fontSize: 20,
        color: "yellow"
    }    
})

export default estilos